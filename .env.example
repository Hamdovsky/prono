# â”€â”€â”€ Server â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
PORT=3001
NODE_ENV=development

# â”€â”€â”€ Authentication â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
API_SECRET_KEY=CHANGER_MOI

# â”€â”€â”€ MarchÃ© 1X2 pur (audit P4) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
# true = tout verdict 1X2 pur ('1'/'X'/'2') est converti en double chance
# ('1'â†’'1X', '2'â†’'X2', 'X'â†’cÃ´tÃ© probable) Ã  la persistance. L'original est
# conservÃ© dans fullData.originalPrediction. RÃ©activation : prÃ©cision calibrÃ©e
# â‰¥ 42,6 % sur n â‰¥ 200 (scripts/diagnose_1x2.js).
DISABLE_PURE_1X2=false

# â”€â”€â”€ MarchÃ© BTTS (audit BT3) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
# true = masque l'affichage BTTS cÃ´tÃ© frontend (Dashboard/MatchCard/
# MarketTerminal/EdgePanel). Signal mesurÃ© : 50,0% global, 53,4% Ã  65%+
# (n=726 baseline dÃ©rivÃ©e). RÃ©activation (double critÃ¨re) : prÃ©cision
# calibrÃ©e â‰¥55% ET flatRoi/roiEvFiltered BTTS > 0 sur nâ‰¥200 picks Ã©mis.
VITE_DISABLE_BTTS_DISPLAY=false

# â”€â”€â”€ MarchÃ© CORNERS (audit C8) â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
# true = masque l'affichage des picks Corners cÃ´tÃ© frontend (MatchCard/
# EdgePanel). Backtest chronologique sans fuite : hit 51,1% < break-even
# 52,6%, ROI âˆ’4,5% Ã  cote 1,87 sur n=8433 paris 2020+ ; log-loss pire que
# la baseline. RÃ©activation : ROI flat > 0 sur byMarket.CORNERS avec vraies
# cotes (collecte C6/C7) sur nâ‰¥200 picks Ã©mis.
VITE_DISABLE_CORNERS_DISPLAY=true

# â”€â”€â”€ Database â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
DATABASE_URL=postgresql://user:password@host:5432/dbname

# â”€â”€â”€ Maintenance DB â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
# RÃ©tention des matchs non-live (jours) avant purge automatique quotidienne.
STALE_MATCH_RETENTION_DAYS=7
# Expression cron (UTC? non, Europe/Paris) de la purge quotidienne des matchs pÃ©rimÃ©s.
STALE_MATCH_CLEANUP_CRON=0 6 * * *

# â”€â”€â”€ Redis â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
REDIS_URL=rediss://default:token@host:6379

# â”€â”€â”€ External APIs â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€
# (see Render Dashboard for full list)

# â”€â”€â”€ Odds â€” sources 100% GRATUITES (recommandÃ©) â”€â”€â”€â”€â”€â”€
# Sofascore public odds â€” Aucune clÃ© requise. Best-effort (IP du dÃ©ploiement
# doit accÃ©der Ã  api.sofascore.com). ActivÃ© par dÃ©faut.
SOFASCORE_ODDS_ENABLED=true
# DÃ©sactive TOUT accÃ¨s Sofascore (dont le scraper Puppeteer) si bloquÃ© sur Render.
DISABLE_SOFASCORE=false
# Cooldown global (ms) aprÃ¨s un blocage 403/429 â€” par dÃ©faut 8 min. Augmenter
# si l'IP du dÃ©ploiement se fait bannir.
SOFASCORE_COOLDOWN_MS=480000

# Odds API.io â€” plan GRATUIT (500 requÃªtes/mois environ), clÃ© requise.
ODDSAPI_IO_ENABLED=true
ODDSAPI_IO_KEY=

 # â”€â”€â”€ Sources FIXTURES/Elo â€” 100% GRATUITES (aucune clÃ©) â”€â”€â”€â”€â”€â”€
 # Livescore â€” fournisseur PRINCIPAL de fixtures de l'orchestrateur rÃ©silient
 # (config/sources/livescore.js), J..J+2, sans clÃ©. =false pour dÃ©sactiver.
 LIVESCORE_ENABLED=true
# LIVESCORE_RETRIES : tentatives supplementaires (reseau/5xx/429/payload invalide) par
# date livescore avant de marquer la source en echec. Defaut 2 ; =0 pour desactiver.
LIVESCORE_RETRIES=2
 # Sofascore-via-PixelRAG (plugin sofascore-py) â€” OPT-IN : endpoints par date
 # Sofascore actuellement 404/WAF (2026-09-09). =true uniquement quand
 # /fixtures de PixelRAG renvoie success=true (GET :30002/fixtures/DATE).
 PIXELRAG_FIXTURES_ENABLED=false
 # PrÃ©-chauffage du cache visuel (enrich J..J+2 aprÃ¨s chaque scan). =false off.
 VISUAL_WARM_ENABLED=true
 # Sofascore â€” fixture provider JS historique (403 global, cooldown 480s).
 SOFASCORE_ENABLED=false
 # OpenLigaDB â€” ligues allemandes + Champions/Europa League, sans clÃ©.
 OPENLIGADB_ENABLED=true
# SportScore â€” scores en direct, sans clÃ© (10k requÃªtes/jour).
SPORTSCORE_ENABLED=true
# BetExplorer â€” cotes, sans clÃ© (nÃ©cessite un bypass web/Puppeteer local).
BETEXPLORER_ENABLED=true
# Football-Data UK (football-data.co.uk) â€” CSV rÃ©sultats + cotes + stats,
# exÃ©cutÃ© par le pipeline Python (data_pipeline/run_daily.py), sans clÃ©.
# PlanifiÃ© via la tÃ¢che Windows Â« Pronos-DataPipeline Â» (07:00) ou cron.
FOOTBALL_DATA_UK_ENABLED=true
# ClubElo (api.clubelo.com) â€” ratings Elo prÃ©-match, sans clÃ© (mÃªme pipeline).
CLUBELO_ENABLED=true
# NOTE Sofascore : si l'IP du dÃ©ploiement est bannie (403/429), le scraper
# applique SOFASCORE_COOLDOWN_MS puis un bypass Puppeteer. La rotation d'IP
# (proxy/Render) est le seul vrai dÃ©blocage d'un bannissement IP.

# â”€â”€â”€ Odds â€” sources PAYANTES (dÃ©sactivÃ©es par dÃ©faut) â”€â”€
# Met Ã  false pour n'utiliser que les sources gratuites.
BSD_ENABLED=false
THERUNDOWN_ENABLED=false
ODDSPAPI_ENABLED=false
SPORTMONKS_ENABLED=false
APIFOOTBALL_ENABLED=false
BIGBALLS_ENABLED=false
PREDIXSPORT_ENABLED=false
SPORTAPI_ENABLED=false
RAPIDAPI_ENABLED=false
# ClÃ©s payantes (ne pas remplir si vous restez 100% gratuit)
BSD_API_KEY=
THERUNDOWN_KEY=
ODDSPAPI_KEY=
SPORTMONKS_KEY=
APIFOOTBALL_KEY=
BIGBALLS_KEY=

# Audit gel cascade calibration (24/08/2026)
# false = isotonique pre-fix NE s'applique plus aux predictions (reactivation par check_iso_gate.js --activate)
ISO_RUNTIME_APPLY=false
# true = calibrage marches TopPicks en identite tant que <150 echantillons post-cutoff (auto-unfreeze integre)
MARKET_CALIB_IDENTITY=true
# false = calibrage affichage (Promosport/MegaTicket) reactive apres refit propre (check_iso_gate)
PROBA_CALIB_IDENTITY=true

# --- PixelRAG / contexte visuel (audit 2026-09-07) ------------------------
# Serveur vision "lite" local (core/visual_server.py, CLIP/PIL sur CPU)
PIXELRAG_URL=http://127.0.0.1:30002
# Vrai PixelRAG hï¿½bergï¿½ (gratuit, sans clï¿½) ï¿½ retrieval Wikipï¿½dia
PIXELRAG_WIKI_URL=https://api.pixelrag.ai
# TTL du cache visual_context_cache (ms, dï¿½faut 12 h)
VISUAL_TTL_MS=43200000
# Batch captures (scripts/scrapeVisualBatch.js) ï¿½ cron #33
VISUAL_BATCH_LIMIT=100
VISUAL_RATE_MS=3000
# true = dï¿½sactive Sofascore/Puppeteer (Render/Docker sans Chromium)
DISABLE_SOFASCORE=false

# --- Lecteur vision (briefing RAG) ï¿½ provider OpenAI-compatible -----------
# Dï¿½fauts = OpenRouter (minimax-m3:free, fallback gemma-4-31b:free si 429)
VISION_LLM_BASE_URL=https://openrouter.ai/api/v1
VISION_LLM_MODEL=minimax/minimax-m3:free
VISION_LLM_FALLBACK_MODEL=google/gemma-4-31b-it:free
# Clï¿½ (sinon fallback OPENROUTER_API_KEY puis GROQ_API_KEY)
VISION_LLM_API_KEY=
OPENROUTER_API_KEY=
GROQ_API_KEY=
# off/0 = dï¿½sactive les briefings ; budgets mensuel/quotidien
VISUAL_BRIEFING=on
VISUAL_BRIEFING_MAX_MONTHLY=500
VISUAL_BRIEFING_MAX_DAILY=40

# --- Signaux visuels entrants dans l'engine --------------------------------
# Confiance minimale du lecteur pour poser un flag is_missing_* (0.55 dï¿½faut ;
# 1.1 = dï¿½sactiver l'injection)
VISUAL_SIGNAL_MIN_CONFIDENCE=0.55
# Activer le booster visuel stitch_v55_visual.json (aprï¿½s train_v55.py --visual)
USE_V55_VISUAL=0

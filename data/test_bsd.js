const Database = require("better-sqlite3"); const db = new Database("data/tactical.db"); const rows = db.prepare("SELECT id, homeTeam, awayTeam, league, startTimestamp, SUBSTR(fullData,1,200) as fd FROM matches WHERE startTimestamp = 0 AND id LIKE 'bsd_%' LIMIT 5").all(); rows.forEach(r => { console.log("ID:", r.id); console.log("  home:", r.homeTeam, "away:", r.awayTeam, "startTs:", r.startTimestamp); console.log("  fullData:", r.fd); });